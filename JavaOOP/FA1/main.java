// main class
public class main {

    // main method
    public static void main(String[] args) {
        // object

        // are of side object
        AreaOfSide objSide = new AreaOfSide();
        objSide.callInput();
        objSide.callOutput();

        // area of cube object
        AreaOfCube objCube = new AreaOfCube();
        objCube.callInput();
        objCube.callOutput();
    }
}
