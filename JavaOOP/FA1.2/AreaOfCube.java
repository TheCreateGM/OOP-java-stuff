public class AreaOfCube {

    public double calculateSurfaceArea(double length) {
        return 6 * length; // Note: Standard surface area of a cube is 6 * side^2. Using 6 * length as per the provided image.
    }
}
